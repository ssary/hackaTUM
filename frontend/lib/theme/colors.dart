import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor =
      Color(0xFF0057B8); // Replace with Check24's primary blue color
  static const lightGray =
      Color(0xFFF0F0F0); // Background for input fields, etc.
  static const darkGray =
      Color(0xFF757575); // Text color for hints, secondary text
  static const errorRed = Color(0xFFD32F2F); // Error color
}
